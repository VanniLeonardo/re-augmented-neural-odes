% =============================================================================
% STRUCTURE ONLY -- no prose yet.
%
% Each section carries a comment saying what belongs there and which committed
% artifact supplies it. Every number quoted in this paper must come from a
% report script run over a committed CSV (`make figures` prints them all), never
% be typed by hand. Claim IDs (D1-D4, D8, C1-C4) are the repository's, defined in
% REPLICATION_PLAN.md §3; keep them out of the final prose or introduce them
% explicitly, since a reader has not seen them.
%
% Sources: raw numbers + pre-declared refutation conditions in OVERNIGHT_LOG.md;
% departures from the papers in DEVIATIONS.md; copying provenance in PROVENANCE.md.
% =============================================================================

\section{Introduction}
% - Target: Dupont et al. 2019, Augmented Neural ODEs (primary).
% - Secondary: four solver/memory claims of Chen et al. 2018 (C1-C4). Chen's
%   Table 1 is deliberately NOT reproduced -- say so here, not later.
% - Why these claims: the ANODE argument is topological (Prop. 1) and its
%   empirical signature is cost (NFE), which makes faithfulness of the ODE solve
%   a precondition for measuring anything. That is the paper's through-line.
% - One paragraph on what a reader gets: a from-description reimplementation,
%   per-seed data, and a protocol that decides at which tolerance a number counts.

\section{Scope of the replication}
\label{sec:scope}
% - What is in: Prop. 1 crossing flow (D8); toy separation / NFE vs budget (D1);
%   missing-slice generalisation, Fig 9 (D2) + our systematic extension;
%   matched-parameter MNIST (D3) and CIFAR-10 (D4) from Table 1; Chen C1-C4.
% - What is out and why: Rubanova et al. (Latent ODE / ODE-RNN), SVHN,
%   ImageNet, Chen's Table 1. Cite OUT_OF_SCOPE.md reasoning in one sentence.
% - Table \ref{tab:scope}: claim -> original figure/table -> our identifier.

\begin{table}[h]
  \centering
  \caption{Claims selected for replication. TODO: fill.}
  \label{tab:scope}
  \begin{tabular}{lll}
    \toprule
    Claim & Original location & Status \\
    \midrule
    % Prop. 1, 1-D crossing flow      & Dupont Fig. 3  & -- \\
    % NFE growth vs training budget   & Dupont Fig. 6  & -- \\
    % Missing-slice generalisation    & Dupont Fig. 9  & -- \\
    % Matched-param MNIST / CIFAR-10  & Dupont Table 1 & -- \\
    % Solver dynamics                 & Chen Fig. 3a-b & -- \\
    % Backward vs forward NFE         & Chen Fig. 3c   & -- \\
    % NFE growth during training      & Chen Fig. 3d   & -- \\
    % Constant-memory adjoint         & Chen Table 1   & -- \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Methods}

\subsection{Reimplementation}
% - Written from the papers as described; no author code copied (PROVENANCE.md).
% - Architectures: toy field (App. F.1.1), conv field with per-convolution time
%   injection (App. F.1.2), augmentation by zero channels, single linear readout.
% - torchdiffeq used as the solver library only.

\subsection{Solver tolerance as a first-class axis}
\label{sec:recon}
% THE METHODOLOGICAL CORE -- this subsection is why several numbers differ from a
% naive re-run, and it should come before any result.
% - A trained Neural ODE field stiffens; at loose tolerance the integrator returns
%   an answer and a plausible NFE while not integrating the field.
% - The reconstruction check: integrate to T, integrate back, require the input
%   recovered to a relative tolerance; the per-row flag recon_ok.
% - Reporting rule: the loosest tolerance at which BOTH compared arms pass on
%   every seed. Rows that fail are reported as data, never as a headline number.
% - Worked example (numbers from OVERNIGHT_LOG): the crossing-flow result at the
%   default 1e-3, where the check fails for all 10 model-seeds.

\subsection{Deviations from the original description}
% - Summarise DEVIATIONS.md; it is the first-class deliverable, this is its digest.
% - The ones that change a number: toy field width, data-space integration (no
%   stem), training tolerance vs the library defaults, epoch budgets, 5 seeds vs
%   Dupont's 20 repeats, flatten-head parameter counts ~0.5-1% above the targets.
% - Table \ref{tab:deviations}: deviation -> why -> expected effect.

\begin{table}[h]
  \centering
  \caption{Deviations from the papers as described. TODO: fill from DEVIATIONS.md.}
  \label{tab:deviations}
  \begin{tabular}{lll}
    \toprule
    Deviation & Reason & Expected effect \\
    \midrule
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Experimental protocol}
% - Pre-declared refutation condition per claim, written before each run
%   (OVERNIGHT_LOG.md); the raw observation decides, not a script's verdict.
% - Seeds (>=5, 10 for the heavy-tailed slice metrics), median/IQR where the
%   distribution is skewed, mean +- s.d. for the image accuracies (Dupont's
%   convention).
% - Every result row records the hardware it ran on; why that mattered (C1).

\subsection{Computational environment and cost}
% - RTX 3090 and A100 MIG slices; pinned container; per-claim cost table from
%   `make help` (D3 1.9 h, D4 4.3 h, slice grid 1.6 h CPU -- all measured).
% - What is and is not deterministic: seeds fix initialisation/shuffling, adaptive
%   solvers and cuDNN are not bit-reproducible across machines; figures rebuilt
%   from committed CSVs are.

\section{Results}
% One subsection per claim. Each states: the original claim, the pre-declared
% refutation condition, the raw observation at a recon-faithful tolerance, and the
% verdict -- including where it is partial. Figures are regenerated by `make figures`.

\subsection{Proposition 1: the one-dimensional crossing flow}
% Dupont Fig. 3 / Prop. 1. Figure: figures/crossing/crossing_flow.png (make fig-d8).
% Note the theory floor (MSE 1.0) and that NODE sits at it rather than below.

\subsection{Toy separation and NFE growth with training budget}
% Dupont §4.2 / Fig. 6. Figures: figures/budget{,_circles}/ (make fig-d1).

\subsection{Generalisation across an unobserved region}
% Dupont §5.1 / Fig. 9. Figure: figures/slice/slice_generalisation.png (make fig-d2).
% Wedge [0, pi/5] verified against the paper text.

\subsubsection{Extension: the missing-slice grid}
% OUR ADDITION, label it clearly as beyond the original paper.
% Augmentation x wedge width x geometry x 10 seeds; figures/slice_grid/ (make
% fig-slice-grid). Report the advantage AND its limits (plateau, loss behaviour,
% ANODE degrading at the widest wedge, non-monotonicity on the second geometry).

\subsection{Matched-parameter image benchmarks}
% Dupont Table 1. MNIST (make fig-d3) and CIFAR-10 (make fig-d4).
% Table \ref{tab:table1}: ours vs Dupont, at the recon-faithful tolerance, with
% the two undershoots stated plainly, and the NFE ratio given as a range across
% tolerances and runs rather than one number.

\begin{table}[h]
  \centering
  \caption{Matched-parameter image results vs Dupont Table 1. TODO: fill from
           \texttt{make fig-d3} and \texttt{make fig-d4}.}
  \label{tab:table1}
  \begin{tabular}{lllll}
    \toprule
    Dataset & Model & Ours & Dupont & Faithful tolerance \\
    \midrule
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Solver dynamics}
% Chen Fig. 3a-b (C1). Figure: figures/c1/solver_dynamics.png (make fig-c1).
% Includes the one pre-declared check that was REFUTED, and where the violation
% lies (non-integrating tolerances). Do not soften this.

\subsection{Backward versus forward NFE}
% Chen Fig. 3c (C2). NOT reproduced; headline withdrawn. Present as a tolerance x
% field surface. Figures: figures/c2/ (make fig-c2). This is a negative result and
% should read as one, with the reason (our adjoint solves a stiffer augmented system).

\subsection{NFE growth during training}
% Chen Fig. 3d (C3). Figures: figures/mnist_nfe/, figures/mnist_stiffening/
% (make fig-c3). Note the faithful tolerance itself tightens as training proceeds.

\subsection{Constant-memory adjoint}
% Chen Table 1 memory column (C4). Corrected experiment -- the coursework swept the
% wrong axis. Figure: figures/c4/memory_vs_nfe.png (make fig-c4).

\section{Discussion}

\subsection{What replicated and what did not}
% Summary table \ref{tab:summary}: per claim, reproduced / partial / not reproduced.
% Be explicit that "partial" means an absolute number missed while the ordering and
% mechanism held.

\begin{table}[h]
  \centering
  \caption{Summary of outcomes. TODO: fill.}
  \label{tab:summary}
  \begin{tabular}{lll}
    \toprule
    Claim & Outcome & Evidence \\
    \midrule
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Why several numbers differ from a naive re-run}
% The tolerance-faithfulness argument, generalised: accuracy proved robust to
% integration error while NFE did not, so cost claims need the check and accuracy
% claims need it stated. This is the transferable methodological contribution.

\subsection{Limitations}
% Seeds below Dupont's 20 repeats; reduced epoch budgets; single reimplementation
% (no independent second implementation); the deviations that could explain the
% two undershoots; results are ours on our hardware, not the authors' code.

\section{Conclusion}
% What a reader should take away about ANODE's claims, and about measuring
% Neural ODE cost at a tolerance that actually integrates the field.

\section*{Code, data and reproduction}
% Repository URL + DOI; `make figures` rebuilds every figure from committed
% per-seed CSVs in about two minutes with no GPU; CI runs that path on every push.

\section*{Author contributions}
% TODO: who did what.

\section*{Acknowledgements}
% TODO: original authors if contacted (state whether they were), compute providers
% (Bocconi HPC), course context.
