# Pinned pip requirements for the ReScience C replication.
# Versions match the committed results (RTX 3090, CUDA 12.1, python 3.11).
#
# torch/torchvision here are the GPU (CUDA 12.1) builds. For a CPU-only install
# (e.g. the smoke pipeline / a reviewer without a GPU), install torch from the
# CPU index first, as the Dockerfile does:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.5.1 torchvision==0.20.1
#
# A fully-resolved lock produced inside the CPU container is in
# requirements-lock-cpu.txt.

torch==2.5.1
torchvision==0.20.1
torchdiffeq==0.2.5
numpy==2.4.3
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.9
pandas==3.0.3
rich==15.0.0
pytest==9.0.3

# Optional experiment-logging backend. The default backend is CSV (no account,
# no network); install wandb only to use NODE_LOGGER=wandb.
# wandb==0.26.1
